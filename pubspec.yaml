name: flutter_use_case
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  connectivity_plus: ^3.0.2
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  kartal: ^2.5.1

dev_dependencies:

  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
